<%= form_for(@news) do |f| %>
  <% if @news.errors.any? %>
    <div class="error_messages">
      <h2><%= pluralize(@news.errors.count, "error") %> prohibited this from being saved:</h2>
      <ul>
      <% @news.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.hidden_field :slug_line, class: "html-editor-inline", placeholder: "Type the slug-line"  %>
  </div>
  <hr/>
  <div class="field">
    <%= f.hidden_field :content, class: "html-editor", placeholder: "Type the content" %>
  </div>
  <hr/>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
